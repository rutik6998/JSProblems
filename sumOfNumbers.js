//Calculate the sum of numbers within an array
const numArray = [1, 12, 53, 40, 15]; 
let sum = 0;  
for (let i = 0; i < numArray.length; i++) 
{     
    sum += numArray[i]; 
}  
console.log("The sum of the numbers within array is:", sum);